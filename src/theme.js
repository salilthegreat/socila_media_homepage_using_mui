import { createTheme } from "@mui/material";
 const theme = createTheme({
    palette:{
        primary:{
            main:"#03a9f4",
            light:"lightblue"
        }
    }
})

export default theme